# Important Federated Learning
For federated learning code. Use python run_all.py to run all 36 experiments at the same time and generate new data and plots. We have however already run it and if you want to save the 200 minutes or so it took to run, just look at the results from the last run in the results folder.

Federated learning is set up using flower (https://flower.ai/docs/framework/index.html) as per assignment instructions. Since the assignment told us to look at how they set it up, we assumed we were allowed to also use those parts, so many parts in the federated learning setup is close to the quickstart setup for pytorch etc. as shown in flower's documentation.

AI usage statement:
    Some AI in the form of Github Copilot has been used for minor bugfixing and corrections, mainly for the federated learning part of the assignment.

For requirements, See imports in each file (if u want to run it yourself that is, otherwise just look at results folder or output blocks in the notebook).
